<!-- generated by `just docs` from tests/cmd/hooks/ — do not hand-edit -->

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install codex'
added the PreToolUse hook
added the SessionStart hook
added the SubagentStart hook
added the PostToolUse hook
hook: installed, not yet approved · open Codex and choose 'Trust all and continue' when prompted, press t in the hooks browser, or pass --dangerously-bypass-hook-trust for one run
For reading, finding and editing files, use `lets` (run `lets guide` once) instead of `cat`,
`grep` or `sed -n`. It reads several files or ranges in one call, returns bounded numbered
output, and its edits return the changed region — so do not follow a `lets` call with a `cat` or
`sed -n` to check the result.
an unapproved hook is skipped entirely · Codex runs the command unhooked, never through lets, until it is approved

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export CODEX_HOME='\'''\''; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install codex >/dev/null; test -f "$HOME/.codex/hooks.json" && echo hooks-json-under-home-codex; test ! -e hooks.json && echo no-hooks-json-in-cwd; ls -A'
hooks-json-under-home-codex
no-hooks-json-in-cwd
home

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export CODEX_HOME="$(pwd)/codex-config"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install codex >/dev/null; test -f "$CODEX_HOME/hooks.json" && echo hooks-json-at-codex-home; test ! -e "$(pwd)/.codex" && echo no-dot-codex-in-working-tree; test ! -e "$HOME/.codex" && echo no-dot-codex-under-home; test ! -e "$CODEX_HOME/AGENTS.md" && echo no-agents-md-written'
hooks-json-at-codex-home
no-dot-codex-in-working-tree
no-dot-codex-under-home
no-agents-md-written

```

```console
$ sh -c 'export HOME="$(pwd)/home"; export CODEX_HOME=codex-config; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install codex; status=$?; ls -A; exit "$status"'
? 64
CODEX_HOME=codex-config is a relative path · set it to an absolute path, or unset it to install under ~/.codex
ERROR_CODE=usage

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install codex; hash1=$(sha256sum "$HOME/.codex/hooks.json" | cut -d'\'' '\'' -f1); "$CARGO_BIN_EXE_lets" hooks install codex; hash2=$(sha256sum "$HOME/.codex/hooks.json" | cut -d'\'' '\'' -f1); if [ "$hash1" = "$hash2" ]; then echo '\''hooks.json byte-identical across reinstall'\''; else echo '\''hooks.json changed across reinstall'\''; fi'
added the PreToolUse hook
added the SessionStart hook
added the SubagentStart hook
added the PostToolUse hook
hook: installed, not yet approved · open Codex and choose 'Trust all and continue' when prompted, press t in the hooks browser, or pass --dangerously-bypass-hook-trust for one run
For reading, finding and editing files, use `lets` (run `lets guide` once) instead of `cat`,
`grep` or `sed -n`. It reads several files or ranges in one call, returns bounded numbered
output, and its edits return the changed region — so do not follow a `lets` call with a `cat` or
`sed -n` to check the result.
an unapproved hook is skipped entirely · Codex runs the command unhooked, never through lets, until it is approved
the PreToolUse hook was already installed
the SessionStart hook was already installed
the SubagentStart hook was already installed
the PostToolUse hook was already installed
hook: installed, not yet approved · open Codex and choose 'Trust all and continue' when prompted, press t in the hooks browser, or pass --dangerously-bypass-hook-trust for one run
For reading, finding and editing files, use `lets` (run `lets guide` once) instead of `cat`,
`grep` or `sed -n`. It reads several files or ranges in one call, returns bounded numbered
output, and its edits return the changed region — so do not follow a `lets` call with a `cat` or
`sed -n` to check the result.
an unapproved hook is skipped entirely · Codex runs the command unhooked, never through lets, until it is approved
hooks.json byte-identical across reinstall

```

```console
$ sh -c 'export HOME="$(pwd)/home"; export PATH="$(pwd)/empty"; "$CARGO_BIN_EXE_lets" hooks install codex; status=$?; if [ -e "$HOME/.codex" ]; then echo unexpected-codex-dir-exists; else echo no-codex-dir-created; fi; exit "$status"'
? 1
no-codex-dir-created
...
ERROR_CODE=not_on_path

```

```console
$ sh -c 'export HOME="$(pwd)/home"; export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install codex; status=$?; if [ -e "$HOME/.codex" ]; then echo unexpected-codex-dir-exists; else echo no-codex-dir-created; fi; exit "$status"'
? 1
no-codex-dir-created
a different `lets` comes first on PATH at [CWD]/fake-bin/lets — this one is [..]/lets · remove the other, or put this one's directory ahead of it on PATH
ERROR_CODE=path_conflict

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; f="$HOME/.codex/hooks.json"; before=$(sha256sum "$f" | cut -d'\'' '\'' -f1); export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install codex >/dev/null; "$CARGO_BIN_EXE_lets" hooks uninstall codex; "$CARGO_BIN_EXE_lets" hooks uninstall codex; after=$(sha256sum "$f" | cut -d'\'' '\'' -f1); if [ "$before" = "$after" ]; then echo '\''hooks.json byte-identical after install then uninstall'\''; else echo '\''hooks.json changed'\''; fi'
removed the PreToolUse hook
removed the SessionStart hook
removed the SubagentStart hook
removed the PostToolUse hook
nothing to remove
hooks.json byte-identical after install then uninstall

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install claude-code'
added the PreToolUse hook
added the SubagentStart hook
added the SessionStart hook
added the PostToolUse hook

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install claude-code; hash1=$(sha256sum "$HOME/.claude/settings.json" | cut -d'\'' '\'' -f1); "$CARGO_BIN_EXE_lets" hooks install claude-code; hash2=$(sha256sum "$HOME/.claude/settings.json" | cut -d'\'' '\'' -f1); if [ "$hash1" = "$hash2" ]; then echo '\''settings.json byte-identical across reinstall'\''; else echo '\''settings.json changed across reinstall'\''; fi'
added the PreToolUse hook
added the SubagentStart hook
added the SessionStart hook
added the PostToolUse hook
the PreToolUse hook was already installed
the SubagentStart hook was already installed
the SessionStart hook was already installed
the PostToolUse hook was already installed
settings.json byte-identical across reinstall

```

```console
$ sh -c 'export HOME="$(pwd)/home"; export PATH="$(pwd)/empty"; "$CARGO_BIN_EXE_lets" hooks install claude-code; status=$?; if [ -e "$HOME/.claude" ]; then echo unexpected-claude-dir-exists; else echo no-claude-dir-created; fi; exit "$status"'
? 1
no-claude-dir-created
...
ERROR_CODE=not_on_path

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$(pwd)/fake-bin:$PATH"; "$CARGO_BIN_EXE_lets" hooks install claude-code'
added the PreToolUse hook
added the SubagentStart hook
added the SessionStart hook
added the PostToolUse hook

```

```console
$ sh -c 'export HOME="$(pwd)/home"; export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install claude-code; status=$?; if [ -e "$HOME/.claude" ]; then echo unexpected-claude-dir-exists; else echo no-claude-dir-created; fi; exit "$status"'
? 1
no-claude-dir-created
a different `lets` comes first on PATH at [CWD]/fake-bin/lets — this one is [..]/lets · remove the other, or put this one's directory ahead of it on PATH
ERROR_CODE=path_conflict

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; f="$HOME/.claude/settings.json"; head -n 12 "$f" > before; "$CARGO_BIN_EXE_lets" hooks install claude-code >/dev/null; if head -n 12 "$f" | cmp -s - before; then echo unrelated-entry-byte-identical; else echo unrelated-entry-changed; fi; grep -c -F '\''"command": "if command -v lets >/dev/null 2>&1; then lets hook classify; fi"'\'' "$f"'
unrelated-entry-byte-identical
2

```

```console
$ sh -c 'export HOME="$(pwd)/home"; "$CARGO_BIN_EXE_lets" hooks uninstall claude-code; f="$HOME/.claude/settings.json"; echo user hooks: $(grep -c -F userhookmarker "$f"); echo lets hooks: $(grep -c -F '\''lets hook classify'\'' "$f"); echo matchers: $(grep -c -F '\''"matcher": "Bash"'\'' "$f")'
removed the PreToolUse hook
user hooks: 1
lets hooks: 0
matchers: 1

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; f="$HOME/.claude/settings.json"; before=$(sha256sum "$f" | cut -d'\'' '\'' -f1); export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install claude-code >/dev/null; PATH=/nonexistent "$CARGO_BIN_EXE_lets" hooks uninstall claude-code; after=$(sha256sum "$f" | cut -d'\'' '\'' -f1); if [ "$before" = "$after" ]; then echo '\''settings.json byte-identical after install then uninstall'\''; else echo '\''settings.json changed'\''; fi'
removed the PreToolUse hook
removed the SubagentStart hook
removed the SessionStart hook
removed the PostToolUse hook
settings.json byte-identical after install then uninstall

```

```console
$ sh -c 'set -e; export HOME="$(pwd)/home"; export PATH="$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; "$CARGO_BIN_EXE_lets" hooks install claude-code >/dev/null; "$CARGO_BIN_EXE_lets" hooks uninstall claude-code >/dev/null; "$CARGO_BIN_EXE_lets" hooks uninstall claude-code'
nothing to remove

```

```console
$ sh -c 'export HOME="$(pwd)/home"; cp "$HOME/.claude/settings.json" before; "$CARGO_BIN_EXE_lets" hooks uninstall claude-code; status=$?; if cmp -s before "$HOME/.claude/settings.json"; then echo settings.json-untouched; else echo settings.json-changed; fi; exit "$status"'
? 7
settings.json-untouched
...
ERROR_CODE=io_error

```
