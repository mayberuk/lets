<!-- generated by `just docs` from tests/cmd/update/ — do not hand-edit -->

```console
$ sh -c 'export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; export FAKE_CURL_LOG="$(pwd)/curl-calls"; export FAKE_LATEST_TAG="v$("$CARGO_BIN_EXE_lets" --version | cut -d'\'' '\'' -f2)"; "$CARGO_BIN_EXE_lets" update --check'
lets [VERSION] is the latest release

```

```console
$ sh -c 'export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; export FAKE_CURL_LOG="$(pwd)/curl-calls"; export FAKE_LATEST_TAG=v999.0.0; "$CARGO_BIN_EXE_lets" update --check --json'
? 1
{"current":"[VERSION]","latest":"999.0.0","update_available":true}
lets [VERSION] · latest 999.0.0 · run `lets update`
ERROR_CODE=update_available

```

```console
$ sh -c 'export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; export FAKE_CURL_LOG="$(pwd)/curl-calls"; export FAKE_LATEST_TAG=nightly; "$CARGO_BIN_EXE_lets" update --check'
? 7
update failed: the latest release tag `nightly` is not vMAJOR.MINOR.PATCH
ERROR_CODE=update_failed

```

```console
$ sh -c 'export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; export FAKE_CURL_LOG="$(pwd)/curl-calls"; export FAKE_LATEST_TAG=v999.0.0; "$CARGO_BIN_EXE_lets" update --check'
? 1
lets [VERSION] · latest 999.0.0
lets [VERSION] · latest 999.0.0 · run `lets update`
ERROR_CODE=update_available

```

```console
$ sh -c 'export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; export FAKE_CURL_LOG="$(pwd)/curl-calls"; unset FAKE_LATEST_TAG; "$CARGO_BIN_EXE_lets" update --check'
? 7
curl: (6) Could not resolve host: github.com
update failed: resolving the latest release from https://github.com/mayberuk/lets/releases/latest failed: curl exit status: 6
ERROR_CODE=update_failed

```

```console
$ sh -c 'export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; export FAKE_CURL_LOG="$(pwd)/curl-calls"; export FAKE_LATEST_TAG="v$("$CARGO_BIN_EXE_lets" --version | cut -d'\'' '\'' -f2)"; "$CARGO_BIN_EXE_lets" update; cat curl-calls'
lets [VERSION] is the latest release
--proto =https --tlsv1.2 --fail -sSI https://github.com/mayberuk/lets/releases/latest

```

```console
$ sh -c 'export PATH="$(pwd)/fake-bin:$(dirname "$CARGO_BIN_EXE_lets"):$PATH"; export FAKE_CURL_LOG="$(pwd)/curl-calls"; export FAKE_LATEST_TAG="v$("$CARGO_BIN_EXE_lets" --version | cut -d'\'' '\'' -f2)"; "$CARGO_BIN_EXE_lets" update --force; echo lookups: $(grep -c -e -sSI curl-calls || true); echo downloads: $(grep -c -e '\'' -o '\'' curl-calls || true); tail -n 1 curl-calls'
replaced with the latest release
lookups: 0
downloads: 1
installer ran

```
