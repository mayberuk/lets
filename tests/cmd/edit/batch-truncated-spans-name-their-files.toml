# finding 5: a batch's `RegionGap` gap used to name no file when more than one result truncated a
# span; each gap now carries the path the way `--all`'s multi-span gaps already do.
bin.name = "lets"
args = ["edit", "--from", "-"]
