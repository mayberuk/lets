# spec.md § edit's batch example inserts `"new":"import x from 'y'\n"`: one line. The insert
# supplies the terminator itself, so the `.out` tree has no blank line after the new one.
bin.name = "lets"
args = ["edit", "m.py", "--insert-after", "@^import", "--new", "import sys\n"]
