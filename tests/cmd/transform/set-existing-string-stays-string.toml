# spec.md § transform, typed --set: `version` already holds the string "3.9", so `3.10` is written
# as the string "3.10", never the number 3.1. set-existing-number-is-parsed is the control.
bin.name = "lets"
args = ["transform", "c.json", "--set", "version=3.10"]
