# base fixture

The three-file tree the sandbox copies per case. Not the generated corpus.

## Bottom line

Small enough to read whole, structured enough to carry a symbol, a heading and a nested key.
