COMMAND
cat -n a.go; grep -n -i x a.go
===END===
VERDICT rewrite
REASON `cat -n a.go` ran as `lets show a.go --all`; `grep -n -i x a.go` ran as `lets find -i 'x' a.go`; lets show reads several files and ranges in one call; lets find returns every hit numbered and grouped by file.
REPLACEMENT
run: lets show a.go --all; lets find -i 'x' a.go
===END===
COMPARE search
