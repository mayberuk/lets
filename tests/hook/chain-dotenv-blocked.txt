COMMAND
cat src/a.ts && cat .env && git status
===END===
VERDICT block
REASON lets show reads several files and ranges in one call.
REPLACEMENT
run: lets show src/a.ts --all && lets show .env && git status
===END===
COMPARE chain
