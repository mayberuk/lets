COMMAND
grep -n a src/grep.txt | sort | head -4; cat src/a.ts 2>/dev/null
===END===
VERDICT block
REASON lets show reads several files and ranges in one call.
REPLACEMENT
run: lets show src/a.ts
===END===
COMPARE read
ORACLE
# The pipeline reads stdin through head, which no lets verb replaces; the replacement stands in for the cat alone.
cat src/a.ts
===END===
